<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Telegram Mini Game</title>
    <script src="https://telegram.org/js/telegram-web-app.js"></script>
    <style>
        :root {
            --tg-theme-bg: #1a1a1a;
            --tg-theme-text: #ffffff;
            --tg-theme-button: #248bdf;
        }
        body {
            background-color: var(--tg-theme-bg);
            color: var(--tg-theme-text);
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            margin: 0;
            display: flex;
            flex-direction: column;
            align-items: center;
            user-select: none;
        }
        .user-info {
            margin-top: 20px;
            text-align: center;
        }
        .user-info img {
            width: 60px;
            border-radius: 50%;
            border: 2px solid var(--tg-theme-button);
        }
        .score-container {
            font-size: 40px;
            font-weight: bold;
            margin: 20px 0;
        }
        #coin {
            width: 200px;
            height: 200px;
            background: radial-gradient(circle, #fbc02d, #f57f17);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 80px;
            box-shadow: 0 10px 20px rgba(0,0,0,0.3);
            cursor: pointer;
            transition: transform 0.1s;
        }
        #coin:active { transform: scale(0.95); }
        
        .leaderboard {
            width: 90%;
            margin-top: 30px;
            background: rgba(255,255,255,0.1);
            border-radius: 15px;
            padding: 15px;
        }
        .leaderboard h3 { margin-top: 0; text-align: center; }
        .lb-item {
            display: flex;
            justify-content: space-between;
            padding: 8px 0;
            border-bottom: 1px solid rgba(255,255,255,0.05);
        }
    </style>
</head>
<body>

    <div class="user-info">
        <div id="user-photo"></div>
        <h2 id="user-name">Loading...</h2>
    </div>

    <div class="score-container">
        💰 <span id="score">0</span>
    </div>

    <div id="coin">🪙</div>

    <div class="leaderboard">
        <h3>🏆 Leaderboard (Top 3)</h3>
        <div id="lb-list">
            <div class="lb-item"><span>1. Player One</span> <span>5000</span></div>
            <div class="lb-item"><span>2. Player Two</span> <span>3500</span></div>
            <div class="lb-item"><span>3. <span id="current-user-lb">You</span></span> <span id="current-user-score">0</span></div>
        </div>
    </div>

    <script>
        const tg = window.Telegram.WebApp;
        const scoreElement = document.getElementById('score');
        const lbScoreElement = document.getElementById('current-user-score');
        const coin = document.getElementById('coin');
        
        let score = 0;

        // টেলিগ্রাম থেকে ইউজার ডাটা নেওয়া
        const user = tg.initDataUnsafe?.user;

        if (user) {
            document.getElementById('user-name').innerText = `Hello, ${user.first_name}!`;
            document.getElementById('current-user-lb').innerText = user.first_name;
            if (user.photo_url) {
                document.getElementById('user-photo').innerHTML = `<img src="${user.photo_url}" />`;
            }
        }

        tg.expand(); // অ্যাপ ফুল স্ক্রিন করা

        coin.onclick = () => {
            score++;
            scoreElement.innerText = score;
            lbScoreElement.innerText = score;
            
            // মোবাইল ভাইব্রেশন
            tg.HapticFeedback.impactOccurred('medium');
        };

        // মেইন বাটন সেটআপ (টেলিগ্রামের নিচে বড় নীল বাটন)
        tg.MainButton.text = "Save Progress";
        tg.MainButton.show();
        tg.MainButton.onClick(() => {
            tg.showAlert(`Great job ${user?.first_name}! Your score ${score} is saved.`);
        });

    </script>
</body>
</html>